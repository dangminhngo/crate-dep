import { styled } from '@/stitches.config'

const Container = styled('div', {
  mx: 'auto',
  px: '$4',
  maxW: '$xl',
  display: 'flex',
  alignItems: 'center',
  gap: '$8',
})

export default Container
